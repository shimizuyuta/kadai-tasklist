<h1>メッセージ一覧</h1>
<ul>
    <% @tasks.each do |task| %>
    <li>
       <%=link_to task.id, task %> :<%= task.content %>
    </li>
    <% end %>
    <%= link_to "新規タスク投稿", new_task_path %>
    <%=link_to "一覧に戻る",tasks_path%>
</ul>

